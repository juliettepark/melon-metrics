import 'package:flutter/material.dart';

class SettingsViewTemp extends StatelessWidget {
  const SettingsViewTemp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('This is Settings View');
  }
}