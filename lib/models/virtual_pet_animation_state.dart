/// This class simply defines the possible states or actions that the pet could take
/// Inlcuding idle, wallking forward/ back, jumping, dying and being sad.
enum VirtualPetAnimationState {
  idle,
  walkForward,
  walkBackward,
  jump,
  death,
  sad
}
